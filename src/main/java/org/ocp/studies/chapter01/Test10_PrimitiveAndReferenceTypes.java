package org.ocp.studies.chapter01;

public class Test10_PrimitiveAndReferenceTypes {

    String reference = "hello";
    int len = reference.length();
    // int bad = len.length(); // DOESN'T COMPILE

    // int value = null;          // DOESN'T COMPILE

    String  name = null;

}
